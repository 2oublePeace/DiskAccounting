spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/main_service
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
server:
  port: 8081